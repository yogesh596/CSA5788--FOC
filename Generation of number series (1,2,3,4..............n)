#include<stdio.h>
int main()
{
          int p, N;
          printf("Input value of N:");
          scanf("%d", &N);
 
       for(p=1; p<=N; p++)
       {
             printf("%d",p);
       }
 return 0; 
}
